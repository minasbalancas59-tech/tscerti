════════════════════════════════════════════════════════════════
 MODELO 4 — ENSAIOS EM DUAS FAIXAS (VERSÃO CORRIGIDA)
 01/09/2026
════════════════════════════════════════════════════════════════

⚠ IMPORTANTE — LEIA ANTES
─────────────────────────
O patch anterior (modelo4-faixas.patch) tinha um erro de chaves e
QUEBROU O BUILD DO WORKER. Se você já o aplicou, o worker está sem
compilar e nenhum PDF é gerado.

Este pacote contém os DOIS commits: a reorganização em duas faixas
e a correção. Aplique sobre o estado ANTERIOR ao patch quebrado.

Se você já aplicou o modelo4-faixas.patch e deu commit, reverta
primeiro:

    cd /root/cert-saas
    git log --oneline -3          # veja o commit "Modelo 4: ensaios em duas faixas"
    git revert --no-edit HEAD     # se ele for o último
    # depois aplique este pacote normalmente


O QUE MUDA
──────────
O bloco ENSAIOS deixa de ser duas colunas lado a lado:

  ┌─────────────────────┬─────────────────────┐
  │ SENSIBILIDADE       │ REPETIBILIDADE      │
  ├─────────────────────┴─────────────────────┤
  │ EXCENTRICIDADE (largura inteira)          │
  └───────────────────────────────────────────┘

· Diagramas da excentricidade: 150 -> 230 pt.
· Linha "Referência (pos. 1)": 6,5 -> 8 pt.
· Paddings reduzidos para compensar a altura extra das duas
  faixas empilhadas (moldura 6->5, espaçamento 4->3,
  células 3,5->2,5), evitando estourar para a segunda página.


COMANDOS
────────
    cd /root/cert-saas
    unzip -o modelo4-faixas-corrigido.zip
    git apply --check modelo4-faixas-corrigido.patch
    git apply modelo4-faixas-corrigido.patch
    git add -A
    git commit -m "Modelo 4: ensaios em duas faixas"
    git push
    ./atualizar.sh --build
    rm -f modelo4-faixas-corrigido.zip

Se o "git apply --check" reclamar, PARE e avise antes de seguir.
Não há migration nesta atualização.


COMO CONFERIR
─────────────
O build tem que terminar SEM erro (o anterior falhava em
GeradorPdf.cs com "} expected"). Depois regere o MB-2026/0094:
  · sensibilidade e repetibilidade lado a lado em cima;
  · excentricidade sozinha embaixo, diagramas maiores;
  · UMA página só.
