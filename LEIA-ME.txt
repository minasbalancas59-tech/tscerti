════════════════════════════════════════════════════════════════
 COBRANÇA — REFEITO DO ZERO, VERIFICADO CONTRA O SERVIDOR REAL
 09/09/2026
════════════════════════════════════════════════════════════════

⚠ SOBRE A TENTATIVA ANTERIOR
──────────────────────────────
O commit "3e23647" que apareceu no seu histórico NÃO tinha o código
de verdade — o "git apply" daquela vez tinha falhado, e o que foi
comitado por engano foram só os arquivos .zip/LEIA-ME que sobraram
na pasta. O atraso automático continuou ativo até agora.

Desta vez eu reclonei o GitHub, confirmei exatamente o que está no
servidor de verdade, e reconstruí tudo em cima disso — testado com
um verificador de sintaxe antes de gerar o patch.


O QUE ESTE PATCH FAZ
──────────────────────

1. DESLIGA o e-mail automático de "mensalidade em aberto".
   Continua existindo o e-mail de LEMBRETE (5 dias antes de vencer)
   — só o de ATRASO (depois de vencido) deixou de ser automático.

2. TELA "💰 Inadimplentes" no painel do super-admin.
   Lista vencidas, com dias de atraso, total, e um botão
   "Enviar aviso" por linha (confirmação antes de disparar).
   Empresa sem gestor com e-mail aparece destacada, botão desabilitado.

3. CARD NA TELA INICIAL do painel — o que você pediu.
   Aparece automaticamente quando há alguma pendência, mostrando até
   5 empresas e o total. Clique leva para a tela cheia. Se a busca
   falhar por qualquer motivo, o card simplesmente não aparece — a
   tela principal não quebra por isso.

4. E-MAIL DIÁRIO PARA VOCÊ (super-admin) — o que você pediu.
   Todo dia útil, SE houver alguma cobrança vencida, você recebe um
   resumo por e-mail: quantas, total, e a lista com dias de atraso.
   Dia sem pendência = sem e-mail (não enche a caixa por nada).

5. Aviso de "mensalidade disponível" no dia em que a cobrança nasce
   (dia 1º do mês), com o PIX — isso já tinha sido pedido antes e
   está incluído de novo aqui, garantido.


COMANDOS
────────
    cd /root/cert-saas
    unzip -o cobranca-completo.zip
    git apply --check cobranca-completo.patch

Se o "--check" reclamar de algo, PARE AQUI e me mande a mensagem
exata — não continue.

Se passar limpo:

    git apply cobranca-completo.patch

    docker compose exec -T db psql -U certsaas -d certsaas \
        < db/seed/155_aviso_cobranca_gerada.sql

    git add -A
    git commit -m "Cobranca: desliga atraso automatico, aviso na geracao, tela e digest"
    git push
    ./atualizar.sh --build
    rm -f cobranca-completo.zip

⚠ A MIGRATION VEM ANTES DO BUILD, sem exceção — evita disparar o
  aviso de "disponível" para cobranças antigas de uma vez só.


COMO CONFERIR
─────────────
· Ctrl+Shift+R e olhe a tela inicial do painel: se houver
  mensalidade vencida, o card "💰 X mensalidade(s) em atraso" deve
  aparecer no topo.

· Clique nele (ou no botão "💰 Inadimplentes" no menu): abre a
  lista completa com o botão de envio.

· O e-mail diário só sai quando há pendência — confira no Mailpit
  (via túnel SSH na porta 8025) no dia seguinte, ou force um teste
  manual rodando a consulta da tela e vendo se há dados.

· Confirme que o atraso automático parou de fato:
      grep -n "AVISO DE ATRASO AO CLIENTE" src/Worker/Program.cs
  Deve mostrar o comentário explicando que está desligado, sem o
  código de envio automático abaixo dele.
